APP_ENV=local
API_HOST=0.0.0.0
API_PORT=8000

ARTIFACT_DIR=artifacts/index
# Supported modes: mock, gemini, gemma, groq. Mock is the no-key default.
LLM_MODE=mock
# Gemma mode requires an available Gemma model ID from Google AI Studio / Gemini API.
MODEL_NAME=mock-local

# Used by both gemini and gemma modes.
GEMINI_API_KEY=
GROQ_API_KEY=

DEFAULT_TOP_K=5
DEFAULT_RETRIEVAL_MODE=hybrid
ENABLE_DEBUG=true
ENABLE_SEMANTIC_GATE=false
